# Supply a unique bootstrap secret; never commit your real .env.
ADMIN_PASSWORD=
ADMIN_EMAIL=
DATABASE_URL=sqlite:///./rapidsupporthub.db
UPLOAD_DIR=./data/uploads
APP_ORIGIN=http://localhost:5173
COOKIE_SECURE=false
# Generate once with Fernet.generate_key(); keep private and preserve with backups.
SSO_ENCRYPTION_KEY=
